#pragma once

// ── WiFi ─────────────────────────────────────────────────────
// Rinomina questo file in config.h e inserisci i tuoi dati.
// NON fare commit di config.h — è già in .gitignore.

#define WIFI_SSID     "YOUR_SSID"
#define WIFI_PASSWORD "YOUR_PASSWORD"

// ── OSC ──────────────────────────────────────────────────────
#define OSC_TARGET_IP  "192.168.1.XXX"  // IP del PC con TouchDesigner
#define OSC_PORT       9000
